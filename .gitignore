node_modules/
dist/
.astro/
.DS_Store
*.log
.env
.env.*

# Raw camera originals (~64 MB of HEIC). Keep locally; the pipeline sources the
# converted JPEGs in pics/ instead. See CREDITS.md.
pics/newer_pics/

# Raw camera originals from the early-morning shoot (~12 MB of HEIC). The
# pipeline sources the converted JPEG in pics/ instead. See CREDITS.md.
pics/early_morning/

# Raw camera originals from the intimate/detail shoot (~30 MB of HEIC). The
# pipeline sources the converted JPEG in pics/ instead. See CREDITS.md.
pics/intimate/

# Rejected barrancoli candidates. One is a Macaulay Library / eBird file and is
# copyrighted, so it must not be committed to a public repo. Nothing here is
# used by the site any more. See CREDITS.md.
pics/barrancoli/

# Raw originals from the pine shoot: an 18MP HEIC plus two 9:16 phone videos
# (~16 MB). The pipeline sources the converted pics/pinar-valley.jpg instead.
pics/pine/
